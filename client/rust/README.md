# client/rust

Client-side Rust workspace used by Flutter via FRB.

Source of truth:

- `crates/mobile-bridge`
- `crates/client-core`

## Build

`cargo build --manifest-path crates/mobile-bridge/Cargo.toml`
